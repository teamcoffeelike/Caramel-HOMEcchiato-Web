<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="notice">
	<select id="getNotices"
			parameterType="Page"
			resultType="Notice">
		SELECT *
 		  FROM (SELECT *
                FROM notices
                ORDER BY writeDate DESC)
 		 WHERE ROWNUM BETWEEN (#{currentPage}-1)*10+1
 		   AND #{currentPage}*10
	</select>
	
	<select id="getTotalCount"
			resultType="integer">
		SELECT count(*)
		FROM notices
	</select>
	
	<insert id="insert">
		INSERT INTO notices (id, title, content)
			   VALUES (seq_notices.nextval, #{title}, #{content})
	</insert>
	
	<select id="detail"
			resultType="Notice">
		SELECT *
		  FROM notices
		 WHERE id = #{id}
	</select>
	
	<update id="update">
		UPDATE notices SET title=#{title}, content=#{content}
		 WHERE id = #{id}
	</update>
	
	<delete id="delete">
		DELETE FROM notices
		 WHERE id = #{id}
	</delete>

</mapper>